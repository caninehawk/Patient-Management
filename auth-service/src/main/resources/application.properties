spring.application.name=auth-service

# HTTP server port
server.port=4005

# datasource defaults (overridable via environment variables)
spring.datasource.url=jdbc:postgresql://auth-service-db:5432/db
spring.datasource.username=admin_user
spring.datasource.password=password

spring.jpa.hibernate.ddl-auto=update
spring.sql.init.mode=always

jwt.secret=VGhpcyBpcyBhIHNhbXBsZSBlbmNvZGVkIHNlY3JldCB2YWx1ZQ==
# Note: the above is base64 for "This is a sample encoded secret value"
# For production, set a strong random base64 secret via environment variable, e.g. JWT_SECRET
